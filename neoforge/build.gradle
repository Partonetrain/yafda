plugins {
    id 'multiloader-loader'
    id 'net.neoforged.moddev'
}

neoForge {
    version = neoforge_version
    // Automatically enable neoforge AccessTransformers if the file exists
    def at = project(':common').file('src/main/resources/META-INF/accesstransformer.cfg')
    if (at.exists()) {
        accessTransformers.from(at.absolutePath)
    }
    parchment {
        minecraftVersion = parchment_minecraft
        mappingsVersion = parchment_version
    }
    runs {
        configureEach {
            systemProperty('neoforge.enabledGameTestNamespaces', mod_id)
            ideName = "NeoForge ${it.name.capitalize()} (${project.path})" // Unify the run config names with fabric
        }
        client {
            client()
        }
        data {
            data()
        }
        server {
            server()
        }
    }
    mods {
        "${mod_id}" {
            sourceSet sourceSets.main
        }
    }
}

repositories {
    exclusiveContent {
        forRepository {
            maven {
                name = "Modrinth"
                url = "https://api.modrinth.com/maven"
            }
        }
        filter {
            includeGroup "maven.modrinth"
        }
    }
    maven { url 'https://maven.wispforest.io' }
    maven {
        url "https://maven.su5ed.dev/releases"
    }
    maven {
        url "https://cursemaven.com"
        content {
            includeGroup "curse.maven"
        }
    }
    repositories {
        maven { url = 'https://maven.minecraftforge.net/' }
    }
    maven {
        name = "Curios"
        url = uri("https://maven.theillusivec4.top/")
    }
}

dependencies {
    implementation "maven.modrinth:farmers-delight:1.21.1-1.2.6"
    implementation "maven.modrinth:deeperdarker:1.3.4-neoforge+1.21"
    implementation "maven.modrinth:aether:1.21.1-1.5.3-neoforge"
    implementation "io.wispforest:owo-lib-neoforge:0.12.15-beta.12+1.21"
    implementation "org.sinytra.forgified-fabric-api:forgified-fabric-api:0.107.0+2.0.23+1.21.1"
    implementation "curse.maven:thirst-was-taken-679270:5857373"
    implementation 'com.github.glitchfiend:TerraBlender-neoforge:1.21.1-4.1.0.8'
    implementation 'maven.modrinth:deep-aether:1.21.1-1.1.1'
    implementation 'maven.modrinth:brewin-and-chewin:4.2.3+1.21.1-neoforge'
    implementation 'maven.modrinth:geckolib:QEqpUJ1G'
    runtimeOnly "top.theillusivec4.curios:curios-neoforge:9.4.2+1.21.1"
    //ars nouvaeu modrinth page isn't updated...
    implementation 'maven.modrinth:arsdelight:2.1.5'
    //ars elemental isn't on modrinth...
}

sourceSets.main.resources { srcDir 'src/generated/resources' }